extern crate concurrency_proj;

use concurrency_proj::con::thread;
use concurrency_proj::con::pass;
use concurrency_proj::con::share;


fn main() {
    //thread::kankan();
    //thread::kankan1();
    //thread::kankan2();
    //thread::kankan3();

    //pass::kankan();
    //pass::kankan1();
    //pass::kankan2();

    //share::kankan();
    share::kankan2();
}
